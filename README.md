# Dotfiles
A small repo containing my nvim and tmux dotfiles
# Setup
   ```sh
   git clone git@github.com:reeceomahoney/.dotfiles.git ~/.dotfiles
   cd ~/.dotfiles
   sudo apt install stow
   stow .
   tmux source-file ~/.config/tmux/tmux.conf
   ```
